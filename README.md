# 💰 Finance Telegram Mini App

Финансовый помощник для Telegram с поддержкой семейных аккаунтов, копилок и детальной аналитики.

## 🚀 Возможности

- 💵 **Управление финансами**: Доходы, расходы, баланс
- 🐷 **Копилки**: Две независимые копилки с целями накопления
- 📊 **Аналитика**: Графики и статистика по категориям
- 👥 **Семейный аккаунт**: Несколько пользователей на один аккаунт
- 💬 **Комментарии**: Обсуждение транзакций
- 🌓 **Темная/Светлая тема**: Автоматическое переключение
- 📱 **Полноэкранный режим**: Оптимизация для Telegram
- ⌨️ **UI Клавиатура**: Удобный ввод сумм

## 📁 Структура проекта

```
tg/
├── backend/          # Node.js + Express + PostgreSQL
│   ├── db.js        # Инициализация БД и миграции
│   ├── server.js    # API endpoints
│   └── package.json
│
└── frontend/        # React + Vite + Tailwind CSS
    ├── src/
    │   ├── components/
    │   │   └── FinanceApp.jsx  # Главный компонент
    │   ├── App.jsx
    │   ├── main.jsx
    │   └── index.css
    └── package.json
```

## 🛠️ Установка и запуск

### Backend

```bash
cd backend
npm install

# Создать .env файл
cp .env.example .env
# Заполнить DATABASE_URL в .env

npm start
```

### Frontend

```bash
cd frontend
npm install

# Создать .env файл
cp .env.example .env
# Заполнить VITE_API_URL в .env

npm run dev
```

## 🔧 Переменные окружения

### Backend (.env)
```env
DATABASE_URL=postgres://user:password@host:port/db
NODE_ENV=production
PORT=10000
```

### Frontend (.env)
```env
VITE_API_URL=https://your-backend-url.com
```

## 📊 База данных

### Таблицы:
- **users** - Пользователи и их финансовые данные
- **transactions** - История транзакций
- **linked_telegram_users** - Связанные Telegram аккаунты
- **transaction_comments** - Комментарии к транзакциям

### Поля пользователя:
- Основные: email, first_name, balance, income, expenses
- Копилка 1: savings_usd, goal_savings, goal_name, initial_savings_amount
- Копилка 2: second_goal_name, second_goal_amount, second_goal_savings, second_goal_initial_amount

## 🎨 Технологии

### Backend:
- Node.js + Express
- PostgreSQL
- bcrypt (хеширование паролей)
- CORS

### Frontend:
- React 18
- Vite
- Tailwind CSS
- Lucide React (иконки)
- Chart.js (графики)
- Telegram WebApp API

## 📱 Telegram Bot

Приложение работает как Telegram Mini App. Для запуска:

1. Создать бота через @BotFather
2. Настроить Web App URL
3. Открыть бота и запустить приложение

## 🔐 Безопасность

- Пароли хешируются с bcrypt
- CORS настроен для защиты API
- SSL для PostgreSQL
- Валидация данных на backend

## 📝 API Endpoints

### Аутентификация
- `POST /api/auth` - Регистрация/Вход

### Пользователь
- `GET /api/user/:email` - Получить данные
- `PUT /api/user/:email` - Обновить данные
- `PUT /api/user/:email/savings-settings` - Настройки копилок

### Транзакции
- `POST /api/transactions` - Создать транзакцию
- `DELETE /api/transactions/:id` - Удалить транзакцию

### Telegram
- `POST /api/link-telegram` - Связать Telegram аккаунт
- `GET /api/linked-users/:email` - Получить связанных пользователей
- `DELETE /api/unlink-telegram` - Отвязать аккаунт

### Комментарии
- `POST /api/transactions/:id/comments` - Добавить комментарий
- `GET /api/transactions/:id/comments` - Получить комментарии
- `DELETE /api/comments/:id` - Удалить комментарий

## 🐛 Известные проблемы

Нет критических проблем. Приложение полностью функционально.

## 📈 Планы развития

- [ ] Разбить FinanceApp.jsx на модули
- [ ] Добавить TypeScript
- [ ] Unit и E2E тесты
- [ ] Экспорт данных (CSV, Excel)
- [ ] Уведомления о достижении целей
- [ ] Бюджетирование по категориям
- [ ] Повторяющиеся транзакции

## 👨‍💻 Разработка

```bash
# Backend
cd backend
npm start

# Frontend (dev mode)
cd frontend
npm run dev

# Frontend (build)
npm run build
npm run preview
```

## 📄 Лицензия

MIT

## 🤝 Контакты

Для вопросов и предложений создавайте Issues в репозитории.
