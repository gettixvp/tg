# 🚀 Быстрая миграция - Готовый скрипт

## ✅ Что уже готово:

- ✅ Новая база данных Neon настроена
- ✅ Скрипт миграции создан (`migrate-ready.js`)
- ✅ Все таблицы будут автоматически созданы

## 📋 Что нужно сделать:

### Шаг 1: Получите старую ссылку из Render

1. Зайдите на https://dashboard.render.com
2. Войдите в ваш аккаунт
3. Найдите ваш **PostgreSQL** сервис (не backend!)
4. Откройте его
5. Перейдите в раздел **"Connect"** или **"Info"**
6. Скопируйте **"Internal Database URL"** или **"Connection String"**
   - Обычно выглядит как: `postgres://user:password@dpg-xxxxx-a.oregon-postgres.render.com/dbname`

### Шаг 2: Вставьте ссылку в скрипт

**Вариант A: Прямо в файле (проще)**

1. Откройте файл `tg/backend/migrate-ready.js`
2. Найдите строку:
   ```javascript
   const OLD_DATABASE_URL = process.env.OLD_DATABASE_URL || 'YOUR_OLD_RENDER_URL';
   ```
3. Замените `'YOUR_OLD_RENDER_URL'` на вашу ссылку из Render:
   ```javascript
   const OLD_DATABASE_URL = process.env.OLD_DATABASE_URL || 'postgres://user:password@dpg-xxxxx-a.oregon-postgres.render.com/dbname';
   ```

**Вариант B: Через .env файл (безопаснее)**

1. Создайте файл `tg/backend/.env`
2. Вставьте:
   ```env
   OLD_DATABASE_URL=postgres://user:password@dpg-xxxxx-a.oregon-postgres.render.com/dbname
   ```
3. Сохраните файл

### Шаг 3: Запустите миграцию

```bash
cd tg/backend
node migrate-ready.js
```

Скрипт автоматически:
- ✅ Подключится к старой базе (Render)
- ✅ Подключится к новой базе (Neon)
- ✅ Экспортирует все данные
- ✅ Импортирует в новую базу
- ✅ Проверит результат

### Шаг 4: Обновите DATABASE_URL в Render

1. Зайдите в ваш **Backend** сервис на Render (не PostgreSQL!)
2. Откройте **"Environment"**
3. Найдите переменную `DATABASE_URL`
4. Замените на новый URL:
   ```
   postgresql://neondb_owner:npg_HnsXeph1qi6g@ep-billowing-base-agrjulce-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
   ```
5. Сохраните (Render автоматически перезапустит сервис)

### Шаг 5: Проверьте работу

1. Откройте ваше приложение
2. Войдите в аккаунт
3. Проверьте, что все данные на месте
4. Проверьте транзакции, копилки и т.д.

---

## ⚠️ Важно!

- ✅ **Не удаляйте старую базу сразу** - оставьте на несколько дней для проверки
- ✅ **Сделайте бэкап** перед миграцией (скрипт это делает автоматически)
- ✅ **Проверьте все данные** после миграции

---

## 🆘 Проблемы?

### Ошибка: "Вы не вставили старую ссылку"
- Убедитесь, что вы заменили `YOUR_OLD_RENDER_URL` на реальную ссылку
- Или создайте `.env` файл с `OLD_DATABASE_URL`

### Ошибка подключения к Render
- Проверьте правильность ссылки
- Убедитесь, что база данных на Render активна
- Попробуйте использовать "Internal Database URL" вместо "External"

### Ошибка подключения к Neon
- Проверьте, что база данных создана на Neon
- Убедитесь, что ссылка скопирована полностью

---

## 📞 Готово!

После выполнения всех шагов ваша база данных будет перенесена на Neon! 🎉

